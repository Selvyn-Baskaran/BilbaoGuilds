<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Welcome to Bilbao Guilds</title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="stylesheet" href="/css/navbar.css">
</head>
<body class="<%= guild %>">

  <%- include('partials/navbar', { points, user, online, role }) %>

  <main class="landing">
    <h1>Welcome to Bilbao Guilds</h1>
    <p>Unite with your Guild. Complete The Guild Quests. Climb the ranks and claim victory.  
    Join the fun and prove your Guild is the best!</p>

    <% if (user) { %>
      <a href="/challenges">View Challenges</a>
    <% } else { %>
      <a href="/login.html">Log In to Begin</a>
    <% } %>
  </main>

  <script src="/socket.io/socket.io.js"></script>
  <script>
    const socket = io();

    socket.on("updateOnline", (count) => {
      const onlineEl = document.getElementById("onlineCount");
      if (onlineEl) onlineEl.textContent = count;
    });
  </script>

</body>
</html>