<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= challenge.title %> - Challenge Thread</title>
  <link rel="stylesheet" href="/styles.css" />
  <link rel="stylesheet" href="/css/navbar.css">
</head>
<body class="<%= guild %>">

  <%- include('partials/navbar', { user, guild, points, online }) %>

  <main>
    <h2><%= challenge.title %></h2>
    <p><%= challenge.description %></p>

    <h3>Replies</h3>

      <ul class="comment-list">
        <% (challenge.comments || []).forEach(c => { %>
          <li class="comment">
            <img
  src="<%= c.avatar || '/uploads/default.png' %>" class="comment-avatar <%= (!c.avatar || c.avatar === '/uploads/default.png') ? 'avatar--default' : '' %>" alt="<%= c.user %>" style="width:32px;height:32px;border-radius:50%;object-fit:cover;">
            <div class="comment-body">
              <div>
                <strong><%= c.user %></strong>
                <% if (c.ts) { %>
                  <span style="opacity:.7; font-size:.9em; margin-left:.4rem;">
                    <%= new Date(c.ts).toLocaleString() %>
                  </span>
                <% } %>
              </div>
              <div><%= c.text %></div>

              <% if (c.attachment) { %>
                <div style="margin-top:.5rem;">
                  <% if (/\.(png|jpe?g|gif|webp)$/i.test(c.attachment)) { %>
                    <a href="<%= c.attachment %>" target="_blank" rel="noopener">
                      <img src="<%= c.attachment %>" alt="attachment" style="max-width:220px;border-radius:8px;display:block;">
                    </a>
                  <% } else { %>
                    <a href="<%= c.attachment %>" target="_blank" rel="noopener">View attachment</a>
                  <% } %>
                </div>
              <% } %>
            </div>
          </li>
        <% }) %>
      </ul>

<form method="POST" action="/challenges/<%= challenge.id %>/comment" enctype="multipart/form-data">
  <input name="text" placeholder="Add a reply..." required />
  <input type="file" name="attachment" accept="image/*,.pdf,.zip,.txt" />
  <button type="submit">Post</button>
</form>

</body>
</html>
